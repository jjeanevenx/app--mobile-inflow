rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permissão total para desenvolvimento - REMOVER EM PRODUÇÃO
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

